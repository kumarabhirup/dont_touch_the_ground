/* global p5, Koji */

class Object {
  constructor(x, y, settings = {}) {
    this.x = x
    this.y = y
    this.settings = settings

    // Play here with Matter.js
  }

  show() {
    // Do something here
  }
}
